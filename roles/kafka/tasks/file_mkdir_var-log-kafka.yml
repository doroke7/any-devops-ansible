- name: (file) rm -Rf /var/log/kafka
  file:
    path: /var/log/kafka
    state: absent
    
- name: (file) mkdir /var/log/kafka
  file:
    path: '/var/log/kafka'
    state: 'directory'
    owner: root
    group: root
    mode: '0775'